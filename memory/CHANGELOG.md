# CHANGELOG — Meraaj Network

## 2026-06 — BATCH 2: تكامل المحاسبة مع واجهة المستخدم (مكتمل)
**RBAC (إعادة استخدام محرك معراج نفسه — لا محرك تصريح ثانٍ):**
- 27 صلاحية محاسبية تفصيلية في `rbac.py` (الدليل، السندات قبض/صرف/اعتماد/إلغاء، القيود إنشاء/ترحيل/عكس، الأستاذ، كشف الحساب، التقارير الكاملة، الفترات عرض/إدارة/إقفال/إعادة فتح، السنة إقفال/إعادة فتح، العملات، الروابط، التدقيق، التدقيق الذاتي) + `office.statement.view`. الافتراضي DENY، ولا يحصل أي دور إداري عام على المحاسبة تلقائياً.
- أدوار جاهزة: `accounting_admin`, `accountant`, `cashier`, `reports_user`, `auditor`, `hr_admin` (بلا محاسبة), وتوسيع `finance_manager`.
- Maker–Checker: أُضيفت العمليات المحاسبية عالية الخطورة إلى `DUAL_CONTROL` نفسه (سند صرف، ترحيل قيد، عكس، إقفال/إعادة فتح فترة وسنة، تغييرات هيكلية، العملات) — بلا محرك موافقات ثانٍ.
- تدقيق التغييرات في `db.audit_log` نفسه: `roles_assigned`, `permissions_assigned`, `account_scope_assigned`, `account_scope_removed`.

**نطاق الحسابات (Account Scope) — `accounting/scope.py`:**
- ثلاثة أنماط: `all_accounts` | `selected_accounts` | `account_subtree` (الشجرة تُحل عبر سلسلة الأب الحقيقية لا بادئة الكود)، مخزّنة على نفس مستند `db.user_roles`.
- مُطبَّق في الخادم على: قائمة الدليل، الشجرة، الحساب بالكود/المعرّف، التعديل/التفعيل/الحذف، الأستاذ، السندات، القيود اليدوية. تمرير كود حساب يدوياً لا يوسّع النطاق.
- المستخدم المقيّد **يُمنع** من التقارير المالية الكاملة (منع تقرير شركة مضلّل جزئي) ويُوجَّه للأستاذ/كشف الحساب.
- واجهة إدارة النطاق داخل «الصلاحيات والأمان» (`AdminRoles.js`) بسبب إلزامي.

**قفل الجهة المحاسبية:** `resolve_entity` يرفض أي `entity_id` غير `meraaj-platform` لكل المستخدمين بلا استثناء. لا جهة محاسبية للمكاتب، و`office_id` لا يُترجم إلى `entity_id` أبداً.

**السندات المحاسبية (`accounting/vouchers.py`)**: سند قبض/صرف يُرحَّل عبر النواة فقط (`accounting_vouchers` + قيد بمفتاح `voucher:{id}`)، الصلاحية لكل نوع على حدة، تسجيل منفصل لـ created/approved/posted/cancelled by (معرّف موثّق لا اسم من الواجهة)، الإلغاء بقيد عكسي لا تعديل/حذف، ومنع الاعتماد الذاتي.

**كشف حساب المكتب (`office_statement.py`)**: تجاري بحت من `db.transactions` + المحفظة، رصيد افتتاحي/جاري/ختامي وتصنيف عربي للحركات، وعزل صارم (مستخدم المكتب مثبّت على مكتبه مهما أرسل).

**الواجهات (frontend/src/pages/accounting/)**: الدليل المحاسبي (شجرة + CRUD بحدود النواة)، السندات (تبويبان قبض/صرف)، القيود اليومية (يدوي/تحقق/ترحيل/عكس)، الأستاذ وكشف الحساب، الفترات والإقفال، العملات والمصارفة، ربط الحسابات وخريطة الأحداث، التقارير المالية (ميزان/دخل/مركز)، المطابقة (قراءة فقط بلا زر إصلاح)، التدقيق الذاتي (قراءة فقط)، كشف حساب المكتب.

**القائمة الجانبية**: مجموعات قابلة للطي (الرئيسية مباشرة، العمليات، المالية التجارية، الحسابات، التقارير، إدارة المكاتب والمنظمات، التسويق، النظام والإدارة) — تعدد مجموعات مفتوحة، حفظ الحالة، فتح تلقائي للمجموعة عند وجود ابن نشط، وتصفية لكل ابن بصلاحيته (المجموعة تظهر فقط إذا كان لها ابن مسموح). لم يُحذف أي مسار أو صفحة.

**قرار الصناديق والبنوك**: REUSE/ADAPT — لا شاشة أرصدة موازية. الحقيقة المحاسبية تُقرأ من الدليل/الأستاذ عبر روابط `cash`/`bank`، والأرصدة التجارية تبقى في «المركز المالي». (منع تكرار الأرصدة.)

**QA**: `tests/batch2_rbac_qa.py` → **92/92** (مصفوفة أدوار، API مباشر، قفل الجهة، النطاق بأنماطه، التقارير الكاملة، Maker–Checker، المنفّذون، شاشات القراءة، عزل المكاتب) + انحدار: Core **77/77**، P1 **30/30**، Batch1 **41/41**. كل بيانات الاختبار مُنشأة ومحذوفة داخل السكربت (`@qa-example.com`)، والدليل والفترات أُعيدا إلى حالتهما الأصلية (فارغة).

## 2026-06 — MERAAJ MOBILE: قشرة تطبيق أصلية (المرحلة الأولى مكتملة)
**البنية**: Capacitor + قشرة موبايل مستقلة تماماً داخل نفس مشروع React على مسارات `/m/*` — بلا Layout/Sidebar الإدارة، وبلا WebView لصفحات الموقع، وبلا باك-إند أو محفظة أو حجوزات موازية.
- `mobile_api.py` (`/api/v1/mobile`): `config` (بوابة الإصدار قبل الدخول)، `bootstrap` (تبويبات/خدمات/أعلام ميزات/شارات/محفظة — Server-driven بطلب واحد)، `home`، `tickets/providers`. طبقة قراءة فقط ومشتقة، لا تحسب مالاً.
- `mobile/api/client.js`: طبقة API واحدة؛ إعادة المحاولة للقراءات فقط (502/503/504) ولا تُعاد أي كتابة (لا حجز/شحن مكرر).
- الشاشات: Splash، Login، Register، Forgot (يوجّه للمسار الحقيقي بلا API وهمي)، Home (بطاقات خدمات + رصيد + برامج + أحدث حجوزات)، برامج العمرة (بحث/تصفية/ترتيب) → تفاصيل → حجز (مسافرون → مراجعة → تأكيد)، حجوزاتي (مركز موحّد قابل لأنواع أخرى) → تفاصيل + مسار، المحفظة (حركات/طلبات شحن/سحوبات)، شحن الرصيد، الإشعارات، مبيعاتي، كشف حساب المكتب، حسابي، التذاكر (مكان + عقد API المطلوب فقط).
- Bottom Navigation بخمسة تبويبات مع شارات من `bootstrap`، Bottom Sheets، Skeletons، Empty/Error states، RTL أولاً، بلا جداول سطح مكتب.
- حسابات الإدارة تُحوَّل إلى لوحة الويب: لوحة الإدارة/المحاسبة لا تُنسخ إلى التطبيق.
- APIs المعاد استخدامها: `/auth/login|register|logout`, `/packages`, `/bookings`, `/wallet/*`, `/notifications`, `/office-statement`.
- مؤجّل: حجز التذاكر (بانتظار عقد شركة النقل)، Push Notifications (العلم موجود والنقل غير موصول)، إعادة تعيين كلمة المرور الذاتية (لا Endpoint بعد).

## 2026-06 — جلسة الإغلاق النهائية (Final Consolidation)
**تصحيح جوهري — Admin داخل التطبيق**: أُلغي تحويل حسابات الإدارة إلى `/admin`. الآن كل الأدوار تدخل قشرة التطبيق، والتجربة تُحدَّد في الخادم من الهوية الموثّقة (`bootstrap.experience` = individual | office | admin) لا من قيمة يرسلها العميل.
**Admin Mobile Experience**: `MAdminHome` يُبنى من `bootstrap.admin_sections` المُصفّاة في الخادم حسب RBAC (مجموعة لا تظهر بلا ابن مسموح)، مع تبويبات سفلية خاصة بالإدارة (الرئيسية/العمليات/الحسابات/الإشعارات/حسابي). شاشتان إداريتان فعليتان: مركز الطلبات، ومراجعة طلبات شحن الرصيد (اعتماد/رفض محمي من التكرار في الخادم). الأقسام غير المبنية للجوال أُزيلت من الكتالوج ولم تُعرض كصفحات وهمية.
**المحاسبة في الجوال — للإدارة المخوَّلة فقط**: `AccountingGate` + `bootstrap.accounting.capabilities` (20 قدرة، كل واحدة بصلاحيتها) — المكتب والفرد لا يريان المحاسبة ولا يصلون لـAPIها (403). الشاشات: لوحة المحاسبة، الدليل (قراءة)، السندات (إنشاء/اعتماد/إلغاء بقيد عكسي)، القيود (عرض/عكس)، الأستاذ، التقارير، الفترات (إقفال/إعادة فتح بسبب إلزامي)، المطابقة والتدقيق الذاتي. تستخدم نفس APIs الباتش 2 ونفس النطاق والرقابة المزدوجة — بلا محرك محاسبي موازٍ. الإقفال السنوي بقي على الويب لتقليل المخاطر.
**Forgot Password — مكتمل E2E**: `POST /api/auth/password-reset/request` (ردّ موحّد يمنع Account Enumeration، حدّ ٥ محاولات/١٥ دقيقة، بلا أي ادّعاء بإرسال بريد) + `/confirm` (توكن مُخزَّن بهاش SHA-256، صالح ساعة، لمرة واحدة، يُنهي كل الجلسات). **EXTERNAL DEPENDENCY**: لا مزوّد بريد/SMS مُهيّأ ⇒ تسليم الرمز يتم عبر الإدارة (يُسجَّل في `audit_log` وإشعار إداري).
**إيصال الشحن كصورة — مكتمل**: `POST /api/wallet/topups/receipt` (GridFS `topup_receipts` — نفس آلية الرفع الموجودة، بلا محرك تخزين ثانٍ) بتحقق نوع وحجم (٥MB) + `GET /api/wallet/receipt/{id}` بتصريح ملكية (المكتب المالك أو صلاحية `funds.release`). واجهة الجوال تستخدم كاميرا/معرض الهاتف.
**Push — معمارية جاهزة**: `POST/DELETE /api/v1/mobile/devices` (توكن مربوط بالمستخدم الموثّق) + `mobile/push.js` باستيراد ديناميكي للإضافة وطلب إذن، ومُعطَّل بأمان على الويب أو حين `PUSH_CONFIGURED=false`. **EXTERNAL CONFIGURATION REQUIRED**: مفاتيح FCM/APNs. الإشعارات داخل التطبيق تعمل كما هي.
**صلابة الشبكة/التكرار**: إعادة المحاولة للقراءات فقط · الحجز يدعم `Idempotency-Key` (إعادة الإرسال تُرجع نفس الحجز) · الشحن محمي في الخادم (409 لطلب مكرر بنفس المبلغ) · مراجعة الشحن تُقبل مرة واحدة فقط (status=pending) · السندات بمفتاح idempotency + قفل الزر · كل الأزرار الحساسة تمنع الضغط المزدوج (حماية العميل إضافة لا بديل).
**QA (جولة واحدة)**: `mobile_final_qa.py` **58/58** (تحديد التجربة، أقسام الإدارة، منع المحاسبة على المكتب/الفرد، النطاق، إعادة التعيين، الإيصال والملكية، التكرار، التوكنات، عزل المكاتب، الجلسة المنتهية/غير الصالحة، سحب الصلاحية فوراً) · `batch2_rbac_qa.py` **94/94** · Core **77/77** · P1 **30/30** · Batch1 **41/41**. UI على 390×844: دخول الإدارة → لوحة إدارة الجوال (٤ مجموعات/١٢ عنصراً) → شاشات المحاسبة، ودخول المكتب → ٩ خدمات، ومنع المكتب من مسار المحاسبة (تحويل إلى الرئيسية)، الإيصال، إعادة التعيين، تسجيل الخروج، الروابط العميقة — بلا تجاوز أفقي.
**DB Impact الجديد**: `device_tokens`, `password_reset_tokens` (+TTL منطقي بالتاريخ), GridFS `topup_receipts.*`, وحقل `idempotency_key` على `bookings`. لا Migration ولا Backfill ولا بيانات إنتاج.

## 2026-06 — جلسة تحقق بعد الـ Fork
- تصحيح إعداد اللينتر في الواجهة: إضافة `/app/frontend/eslint.config.js` (ESLint 9 flat config) — كان اللينتر يفشل بسبب غياب ملف الإعداد. لا تغيير في منطق التطبيق.
- تشغيل كل سكربتات QA: Core 77/77 · P1 30/30 · Batch1 41/41 · Batch2 92/92 · Mobile 58/58 (بدون أخطاء).
- ملاحظة تشغيلية: سكربتات Batch2/Mobile تحتاج تحميل متغيرات البيئة قبل التشغيل:
  `cd /app/backend && set -a && . ./.env && set +a && python3 tests/mobile_final_qa.py`
- تحقق بصري: `/m/home` على 390×844 و الصفحة الرئيسية على 1920×800 تعملان بشكل سليم.

## 2026-06 — MERAAJ MOBILE: إعادة تصميم Native UX/UI (بلا تغيير Backend مالي)
**النطاق**: واجهة `/m/*` فقط. لم تُلمس Accounting Core ولا Batch1/Batch2 ولا RBAC ولا الـ APIs ولا Idempotency، ولا أي `data-testid` قائم.

**Design System جديد** `src/mobile/ui/kit.js` (إعادة كتابة كـ superset): TopBar (Large + ظل عند التمرير) · Card · ListRow · KpiCard · SectionTitle · Skeleton(Shimmer) · Empty/Error · Primary/Gold/Ghost/Danger Buttons · Fab · mInput/MField · SearchField · **SheetSelect** (Bottom-Sheet Picker بدل `<select>` الخام) · DateField · **FilePicker** (كاميرا/ملف) · Segmented/Chip · Sheet · StatusPill · Money · **PullToRefresh** · `useAsync` مع **SWR cache** لحفظ حالة التبويبات.
**ملفات جديدة**: `mobile/ui/mobile.css` (انتقالات push/pop، stagger، shimmer، sheet، PTR) · `mobile/ui/haptics.js` (@capacitor/haptics، no-op على الويب).
**Navigation**: نفس الخمسة تبويبات (server-driven) + انتقالات push/pop حسب فهرس الـ history + Badges + حفظ حالة التبويب + **إصلاح زر الرجوع الأندرويدي**: `/m/home` و `/m/login` و `/m` أصبحت جذور التطبيق ولا يُعاد المستخدم أبداً إلى `/dashboard`.
**شاشات أُعيد تصميمها (22)**: MSplash · MLogin · MRegister (خطوتان) · MForgot (خطوات) · MHome (Hero محفظة + شبكة خدمات + Carousel برامج بصور) · MAdminHome (**KPI Cards من `/api/admin/dashboard` الموجود** + إجراءات سريعة + تنبيهات) · MPrograms (بطاقات بصور + تصفية Sheet) · MProgramDetail (Hero + شريط سعر ثابت) · MBookingFlow · MBookings (فلترة حالة + أنواع) · MBookingDetail (Timeline) · MWallet (بطاقة محفظة بتبديل عملة) · MTopup (مبالغ سريعة + Sheet pickers + FilePicker + شاشة نجاح) · MStatement · MSales · MNotifications · MAccount (Settings/Sections) · **MTickets (أُزيلت متطلبات API التقنية من واجهة العميل → شاشة "قريباً" محترمة)** · MAccountingHub (+ **ScopeBanner**: العرض الجزئي لا يظهر كتقرير كامل) · MAccChart (بحث + Chips) · MAccVouchers/MAccJournals/MAccPeriods (**Sheet للسبب بدل window.prompt**) · MAccLedger · MAccReports · MAccAudit · MAdminOrders · MAdminTopups (تأكيد خطوتين للاعتماد/الرفض).
**Backend exceptions**: **لا شيء**. استُخدم `cover_image`/`images` الموجود أصلاً في `/api/packages` و`/v1/mobile/home`، وKPI من `/api/admin/dashboard` الموجود.
**إصلاح جانبي في سكربت QA**: `mobile_final_qa.py` لم يكن يحذف `accounting_periods` المزروعة عند التنظيف (تركت ٣٥ حساباً و١٢ فترة في preview) — أُضيف الحذف ونُظّفت البيانات المتبقية (٠ قيود، لا بيانات مالكة).
**QA**: Core **77/77** · P1 **30/30** · Batch1 **41/41** · Batch2 **94/94** · Mobile **58/58** (المجموع 300/0 FAIL) · ESLint نظيف · فحص بصري على 390×844 لـ 15 شاشة: **صفر تجاوز أفقي**.
